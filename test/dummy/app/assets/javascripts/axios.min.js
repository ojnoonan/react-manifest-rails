/*
 * Dummy axios shim for migration verification in test/dummy.
 */
(function(global) {
  var axios = {
    get: function(url) {
      return Promise.resolve({ data: { ok: true, url: url } });
    }
  };

  global.axios = axios;
})(this);
